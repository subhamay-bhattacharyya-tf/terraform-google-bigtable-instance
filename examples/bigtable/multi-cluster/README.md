# Bigtable Instance with Multiple Clusters

A PRODUCTION Bigtable instance with two clusters across different zones for high availability and replication.

## Usage

```bash
terraform init -backend=false
terraform validate
```
