# Bigtable Instance with Deletion Protection

A PRODUCTION Bigtable instance with `deletion_protection = true` to prevent accidental deletion in production environments.

## Usage

```bash
terraform init -backend=false
terraform validate
```
