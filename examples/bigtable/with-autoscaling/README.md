# Bigtable Instance with Autoscaling

A PRODUCTION Bigtable instance with CPU-based autoscaling enabled (1–5 nodes, 60% CPU target).

## Usage

```bash
terraform init -backend=false
terraform validate
```
